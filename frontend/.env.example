# Copy this to .env.local for local dev (uses localhost backend)
VITE_API_URL=http://localhost:5000/api

# Copy this to .env.production before building the APK or deploying frontend
# Replace with your actual Render backend URL
# VITE_API_URL=https://your-app-name.onrender.com/api
